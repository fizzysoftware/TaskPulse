// Constants for TaskPulse app
// Note: User and task data is now fetched from the backend API

export const PLACEHOLDER_IMAGE = "https://picsum.photos/400/300";

// Default company code (used only as fallback)
export const DEFAULT_COMPANY_CODE = "TEAM2025";
